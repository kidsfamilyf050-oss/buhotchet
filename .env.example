# Railway → PostgreSQL → скопировать DATABASE_URL в Variables
DATABASE_URL=postgresql://user:pass@host:5432/railway

PORT=3000

# GitHub Pages + локальная разработка (через запятую)
CORS_ORIGINS=https://kidsfamilyf050-oss.github.io,http://localhost:3000,http://127.0.0.1:3000
